#!/bin/bash

kitten @ --to unix:/tmp/mykitty launch --tab-title=music --type=tab /usr/bin/ncmpcpp && $MAZ_SCRIPTS_BIN/focus-st

# /usr/bin/tmux new-window /usr/bin/ncmpcpp \
#   && $MAZ_SCRIPTS_BIN/focus-st
